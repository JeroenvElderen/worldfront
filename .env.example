VITE_MAPBOX_ACCESS_TOKEN=pk.your_public_mapbox_token
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2
# Optional OpenStreetMap Overpass mirror used to ground AI jobs in real places:
# OVERPASS_API_URL=https://overpass-api.de/api/interpreter
# Optional when a deployed backend is used instead of the built-in local endpoint:
# VITE_AI_JOBS_ENDPOINT=https://your-backend.example.com/api/jobs
