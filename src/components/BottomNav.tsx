import {MaterialCommunityIcons} from '@expo/vector-icons';import {usePathname,useRouter} from 'expo-router';import {Pressable,StyleSheet,Text,View} from 'react-native';import {useSafeAreaInsets} from 'react-native-safe-area-context';import {colors} from './theme';
const tabs=[['/','map-marker-radius','Map'],['/jobs','briefcase-clock','Jobs'],['/fleet','car-multiple','Fleet'],['/company','office-building','Company']] as const;
export function BottomNav(){const router=useRouter(),path=usePathname(),insets=useSafeAreaInsets();return <View style={[s.nav,{paddingBottom:Math.max(insets.bottom,8)}]}>{tabs.map(([href,icon,label])=>{const active=path===href;return <Pressable key={href} style={s.tab} onPress={()=>router.replace(href)}><MaterialCommunityIcons name={icon} size={23} color={active?colors.primary:colors.muted}/><Text style={[s.label,active&&s.active]}>{label}</Text></Pressable>})}</View>}
const s=StyleSheet.create({nav:{position:'absolute',left:12,right:12,bottom:8,backgroundColor:'rgba(7,17,31,.97)',borderRadius:24,flexDirection:'row',paddingTop:10,borderWidth:1,borderColor:colors.border},tab:{flex:1,alignItems:'center',gap:3},label:{color:colors.muted,fontSize:11,fontWeight:'700'},active:{color:colors.primary}});
