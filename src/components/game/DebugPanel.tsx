import {Pressable,StyleSheet,Text,View} from 'react-native';import {useGameStore} from '@/store/gameStore';import {colors} from '../theme';
export function DebugPanel(){const addCash=useGameStore(s=>s.addCash),addXp=useGameStore(s=>s.addXp),complete=useGameStore(s=>s.completeTrip),generate=useGameStore(s=>s.generateJobs),reset=useGameStore(s=>s.resetGame);if(!__DEV__)return null;return <View style={s.box}><Text style={s.title}>DEVELOPMENT TOOLS</Text><View style={s.row}>{[['+ €10,000',()=>addCash(10000)],['+ 100 XP',()=>addXp(100)],['Complete trip',complete],['Generate jobs',generate],['Reset save',()=>void reset()]].map(([label,fn])=><Pressable key={String(label)} style={s.button} onPress={fn as ()=>void}><Text style={s.text}>{String(label)}</Text></Pressable>)}</View></View>}
const s=StyleSheet.create({box:{borderWidth:1,borderColor:colors.accent,borderRadius:18,padding:14,marginTop:8},title:{color:colors.accent,fontSize:10,fontWeight:'900',letterSpacing:1.3},row:{flexDirection:'row',flexWrap:'wrap',gap:8,marginTop:12},button:{backgroundColor:colors.card,padding:10,borderRadius:10},text:{color:colors.text,fontSize:12,fontWeight:'700'}});
