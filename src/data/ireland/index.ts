import type { AdministrativeTerritory } from "@/types/game";

// Data is intentionally not substituted with approximate geometry. Follow the
// adjacent README to add the authoritative council-boundary export.
export const irelandAdministrativeTerritories: AdministrativeTerritory[] = [];

